#include <stdio.h>
#include <stdbool.h>

bool IsEven(int num){
    if(num % 2 == 0) return true;
    return false;
}

int main() {
    int first = 5, second = 2;
    
    if(IsEven(first) == IsEven(second)) printf("Doesn't love");
    else printf("Does love");

    return 0;
}
